// Defaults baked into the extension. Invite links override these automatically,
// so friends never need to touch settings. If you want your own build to point
// at your relay out of the box, set DEFAULT_SERVER_URL to your wss:// address.
const WP_CONFIG = {
  DEFAULT_SERVER_URL: "wss://watchparty-relay-9om9.onrender.com",
  DEFAULT_SECRET: "",
};
